<?php

declare(strict_types=1);

namespace Flow\Bridge\Symfony\TelemetryBundle\DependencyInjection;

use Symfony\Component\Config\Definition\Builder\{ArrayNodeDefinition, TreeBuilder};
use Symfony\Component\Config\Definition\ConfigurationInterface;

final class Configuration implements ConfigurationInterface
{
    public function getConfigTreeBuilder() : TreeBuilder
    {
        $treeBuilder = new TreeBuilder('flow_telemetry');
        $rootNode = $treeBuilder->getRootNode();

        $rootNode
            ->children()
                ->arrayNode('resource')
                    ->info('OpenTelemetry Resource configuration (https://opentelemetry.io/docs/specs/semconv/resource/)')
                    ->isRequired()
                    ->children()
                        ->arrayNode('service')
                            ->info('Service-related resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/#service)')
                            ->isRequired()
                            ->children()
                                ->scalarNode('name')
                                    ->info("Logical name of the service (maps to 'service.name' resource attribute)")
                                    ->isRequired()
                                    ->cannotBeEmpty()
                                ->end()
                                ->arrayNode('version')
                                    ->info("Version of the service (maps to 'service.version' resource attribute)")
                                    ->children()
                                        ->enumNode('type')
                                            ->values(['manual', 'package'])
                                            ->isRequired()
                                        ->end()
                                        ->scalarNode('value')
                                            ->info('Version string (for type: manual)')
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('name')
                                            ->info('Package name to resolve version from (for type: package)')
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->scalarNode('namespace')
                                    ->info("Namespace for service.name (maps to 'service.namespace' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('instance_id')
                                    ->info("Unique string ID of service instance (maps to 'service.instance.id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('telemetry_sdk')
                            ->info('Telemetry SDK resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/#telemetry-sdk)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('language')
                                    ->info("The language of the telemetry SDK (maps to 'telemetry.sdk.language' resource attribute)")
                                    ->defaultValue('php')
                                ->end()
                                ->scalarNode('name')
                                    ->info("The name of the telemetry SDK (maps to 'telemetry.sdk.name' resource attribute)")
                                    ->defaultValue('flow-php/telemetry')
                                ->end()
                                ->scalarNode('version')
                                    ->info("The version of the telemetry SDK (maps to 'telemetry.sdk.version' resource attribute)")
                                    ->defaultNull()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('deployment')
                            ->info('Deployment environment resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/deployment-environment/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('environment')
                                    ->info("Name of the deployment environment, e.g. 'staging', 'production' (maps to 'deployment.environment.name' resource attribute)")
                                    ->defaultNull()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('host')
                            ->info('Host resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/host/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('id')
                                    ->info("Unique host ID. For Cloud, this must be the instance_id assigned by the cloud provider (maps to 'host.id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('name')
                                    ->info("Name of the host. On Unix systems, it may contain what the hostname command returns (maps to 'host.name' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('type')
                                    ->info("Type of host. For Cloud, this must be the machine type (maps to 'host.type' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('arch')
                                    ->info("The CPU architecture the host system is running on, e.g. amd64, arm64 (maps to 'host.arch' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->arrayNode('image')
                                    ->info('Host image attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('id')
                                            ->info("VM image ID or host OS image ID (maps to 'host.image.id' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('name')
                                            ->info("Name of the VM image or OS install the host was instantiated from (maps to 'host.image.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('version')
                                            ->info("The version string of the VM image or host OS (maps to 'host.image.version' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('ip')
                                    ->info("Available IP addresses of the host, excluding loopback interfaces (maps to 'host.ip' resource attribute)")
                                    ->scalarPrototype()->end()
                                ->end()
                                ->arrayNode('mac')
                                    ->info("Available MAC addresses of the host, excluding loopback interfaces (maps to 'host.mac' resource attribute)")
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('os')
                            ->info('Operating system resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/os/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('type')
                                    ->info("The operating system type, e.g. windows, linux, darwin (maps to 'os.type' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('name')
                                    ->info("Human readable operating system name, e.g. iOS, Android, Ubuntu (maps to 'os.name' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('version')
                                    ->info("The version string of the operating system, e.g. 14.2.1, 18.04.1 (maps to 'os.version' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('description')
                                    ->info("Human readable OS version information (maps to 'os.description' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('build_id')
                                    ->info("Unique identifier for a particular build of the operating system (maps to 'os.build_id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('process')
                            ->info('Process resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/process/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->integerNode('pid')
                                    ->info("Process identifier (PID) (maps to 'process.pid' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->integerNode('parent_pid')
                                    ->info("Parent Process identifier (PPID) (maps to 'process.parent_pid' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('command')
                                    ->info("The command used to launch the process (maps to 'process.command' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('command_line')
                                    ->info("The full command used to launch the process as a single string (maps to 'process.command_line' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('owner')
                                    ->info("The username of the user that owns the process (maps to 'process.owner' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->arrayNode('executable')
                                    ->info('Process executable attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the process executable (maps to 'process.executable.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('path')
                                            ->info("The full path to the process executable (maps to 'process.executable.path' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('runtime')
                                    ->info('Process runtime attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the runtime of this process (maps to 'process.runtime.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('version')
                                            ->info("The version of the runtime (maps to 'process.runtime.version' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('description')
                                            ->info("An additional description about the runtime (maps to 'process.runtime.description' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('container')
                            ->info('Container resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/container/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('id')
                                    ->info("Container ID. Usually a UUID, as for example used to identify Docker containers (maps to 'container.id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('name')
                                    ->info("Container name used by container runtime (maps to 'container.name' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('command')
                                    ->info("The command used to run the container (maps to 'container.command' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('command_line')
                                    ->info("The full command run by the container as a single string (maps to 'container.command_line' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('image_name')
                                    ->info("Name of the image the container was built on (maps to 'container.image.name' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('image_id')
                                    ->info("Container image ID (maps to 'container.image.id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->arrayNode('image_tags')
                                    ->info("Container image tags (maps to 'container.image.tags' resource attribute)")
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('k8s')
                            ->info('Kubernetes resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/k8s/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('cluster')
                                    ->info('Kubernetes cluster attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the cluster (maps to 'k8s.cluster.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("A pseudo-ID for the cluster, set to the UID of the kube-system namespace (maps to 'k8s.cluster.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('namespace')
                                    ->info('Kubernetes namespace attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the namespace that the pod is running in (maps to 'k8s.namespace.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('node')
                                    ->info('Kubernetes node attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the Node (maps to 'k8s.node.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the Node (maps to 'k8s.node.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('pod')
                                    ->info('Kubernetes pod attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the Pod (maps to 'k8s.pod.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the Pod (maps to 'k8s.pod.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('ip')
                                            ->info("The IP address of the Pod (maps to 'k8s.pod.ip' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('container')
                                    ->info('Kubernetes container attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the Container from Pod specification (maps to 'k8s.container.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->integerNode('restart_count')
                                            ->info("Number of times the container was restarted (maps to 'k8s.container.restart_count' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('deployment')
                                    ->info('Kubernetes deployment attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the Deployment (maps to 'k8s.deployment.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the Deployment (maps to 'k8s.deployment.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('replicaset')
                                    ->info('Kubernetes replicaset attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the ReplicaSet (maps to 'k8s.replicaset.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the ReplicaSet (maps to 'k8s.replicaset.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('statefulset')
                                    ->info('Kubernetes statefulset attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the StatefulSet (maps to 'k8s.statefulset.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the StatefulSet (maps to 'k8s.statefulset.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('daemonset')
                                    ->info('Kubernetes daemonset attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the DaemonSet (maps to 'k8s.daemonset.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the DaemonSet (maps to 'k8s.daemonset.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('job')
                                    ->info('Kubernetes job attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the Job (maps to 'k8s.job.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the Job (maps to 'k8s.job.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                                ->arrayNode('cronjob')
                                    ->info('Kubernetes cronjob attributes')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->scalarNode('name')
                                            ->info("The name of the CronJob (maps to 'k8s.cronjob.name' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                        ->scalarNode('uid')
                                            ->info("The UID of the CronJob (maps to 'k8s.cronjob.uid' resource attribute)")
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('cloud')
                            ->info('Cloud resource attributes (https://opentelemetry.io/docs/specs/semconv/resource/cloud/)')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('provider')
                                    ->info("Name of the cloud provider, e.g. aws, azure, gcp (maps to 'cloud.provider' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('account_id')
                                    ->info("The cloud account ID the resource is assigned to (maps to 'cloud.account.id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('region')
                                    ->info("The geographical region the resource is running (maps to 'cloud.region' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('availability_zone')
                                    ->info("Cloud regions often have multiple, isolated locations known as zones (maps to 'cloud.availability_zone' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('platform')
                                    ->info("The cloud platform in use, e.g. aws_ec2, azure_vm, gcp_compute_engine (maps to 'cloud.platform' resource attribute)")
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('resource_id')
                                    ->info("Cloud provider-specific native identifier, e.g. AWS ARN (maps to 'cloud.resource_id' resource attribute)")
                                    ->defaultNull()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('custom')
                            ->info('Custom resource attributes passed as-is without transformation')
                            ->normalizeKeys(false)
                            ->useAttributeAsKey('name')
                            ->prototype('variable')->end()
                        ->end()
                    ->end()
                ->end()
                ->scalarNode('clock_service_id')
                    ->info('Custom PSR-20 clock service ID. If not provided, uses built-in SystemClock.')
                    ->defaultNull()
                ->end()
                ->arrayNode('context_storage')
                    ->info('Context storage configuration')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->enumNode('type')
                            ->values(['memory', 'service'])
                            ->defaultValue('memory')
                        ->end()
                        ->scalarNode('service_id')
                            ->info('Custom context storage service ID (only for type: service)')
                            ->defaultNull()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('propagator')
                    ->info('Context propagator configuration for distributed tracing')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->enumNode('type')
                            ->info("Propagator type: 'w3c' (W3C TraceContext + Baggage), 'tracecontext' (W3C TraceContext only), 'baggage' (W3C Baggage only), 'service' (custom)")
                            ->values(['w3c', 'tracecontext', 'baggage', 'service'])
                            ->defaultValue('w3c')
                        ->end()
                        ->scalarNode('service_id')
                            ->info('Custom propagator service ID (only for type: service)')
                            ->defaultNull()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('tracer_provider')
                    ->info('TracerProvider configuration. Defaults to void if omitted.')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('sampler')
                            ->info('Trace sampler configuration')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->enumNode('type')
                                    ->values(['always_on', 'always_off', 'trace_id_ratio', 'parent_based', 'service'])
                                    ->defaultValue('always_on')
                                ->end()
                                ->floatNode('ratio')
                                    ->info('Sampling ratio for trace_id_ratio type (0.0 to 1.0)')
                                    ->defaultValue(1.0)
                                    ->min(0.0)
                                    ->max(1.0)
                                ->end()
                                ->scalarNode('service_id')
                                    ->info('Custom sampler service ID (only for type: service)')
                                    ->defaultNull()
                                ->end()
                            ->end()
                        ->end()
                        ->append($this->processorNode('span'))
                    ->end()
                ->end()
                ->arrayNode('meter_provider')
                    ->info('MeterProvider configuration. Defaults to void if omitted.')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->enumNode('temporality')
                            ->info('Aggregation temporality')
                            ->values(['cumulative', 'delta'])
                            ->defaultValue('cumulative')
                        ->end()
                        ->append($this->processorNode('metric'))
                    ->end()
                ->end()
                ->arrayNode('logger_provider')
                    ->info('LoggerProvider configuration. Defaults to void if omitted.')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->append($this->processorNode('log'))
                    ->end()
                ->end()
                ->arrayNode('instrumentation')
                    ->info('Auto-instrumentation configuration')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('http_kernel')
                            ->info('HTTP kernel request tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->arrayNode('exclude_routes')
                                    ->info('Route names to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                                ->booleanNode('context_propagation')
                                    ->info('Enable context propagation from incoming HTTP headers (requires propagator)')
                                    ->defaultTrue()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('console')
                            ->info('Console command tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->arrayNode('exclude_commands')
                                    ->info('Command names to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('messenger')
                            ->info('Messenger tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->booleanNode('context_propagation')
                                    ->info('Enable context propagation across message boundaries (requires propagator)')
                                    ->defaultTrue()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('twig')
                            ->info('Twig template tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->booleanNode('trace_templates')
                                    ->info('Trace template rendering')
                                    ->defaultTrue()
                                ->end()
                                ->booleanNode('trace_blocks')
                                    ->info('Trace block rendering')
                                    ->defaultFalse()
                                ->end()
                                ->booleanNode('trace_macros')
                                    ->info('Trace macro execution')
                                    ->defaultFalse()
                                ->end()
                                ->arrayNode('exclude_templates')
                                    ->info('Template paths to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('http_client')
                            ->info('HTTP client request tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->arrayNode('exclude_clients')
                                    ->info('HTTP client service IDs to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('psr18_client')
                            ->info('PSR-18 HTTP client request tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->arrayNode('exclude_clients')
                                    ->info('PSR-18 client service IDs to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('dbal')
                            ->info('Doctrine DBAL query tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->booleanNode('log_sql')
                                    ->info('Whether to include SQL in span attributes')
                                    ->defaultTrue()
                                ->end()
                                ->integerNode('max_sql_length')
                                    ->info('Maximum SQL length in span attributes (0 = no limit)')
                                    ->defaultValue(1000)
                                    ->min(0)
                                ->end()
                                ->arrayNode('exclude_connections')
                                    ->info('Connection names to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('cache')
                            ->info('Symfony Cache tracing configuration')
                            ->canBeEnabled()
                            ->children()
                                ->arrayNode('exclude_pools')
                                    ->info('Cache pool service IDs to exclude from tracing (supports regex with / delimiters)')
                                    ->scalarPrototype()->end()
                                ->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('tracers')
                    ->info('Named tracer configurations')
                    ->useAttributeAsKey('name')
                    ->arrayPrototype()
                        ->children()
                            ->scalarNode('version')
                                ->info('Instrumentation scope version')
                                ->defaultValue('unknown')
                            ->end()
                            ->scalarNode('schema_url')
                                ->info('Schema URL for semantic conventions')
                                ->defaultNull()
                            ->end()
                            ->arrayNode('attributes')
                                ->info('Additional scope attributes')
                                ->normalizeKeys(false)
                                ->useAttributeAsKey('name')
                                ->prototype('variable')->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('meters')
                    ->info('Named meter configurations')
                    ->useAttributeAsKey('name')
                    ->arrayPrototype()
                        ->children()
                            ->scalarNode('version')
                                ->info('Instrumentation scope version')
                                ->defaultValue('unknown')
                            ->end()
                            ->scalarNode('schema_url')
                                ->info('Schema URL for semantic conventions')
                                ->defaultNull()
                            ->end()
                            ->arrayNode('attributes')
                                ->info('Additional scope attributes')
                                ->normalizeKeys(false)
                                ->useAttributeAsKey('name')
                                ->prototype('variable')->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('loggers')
                    ->info('Named logger configurations')
                    ->useAttributeAsKey('name')
                    ->arrayPrototype()
                        ->children()
                            ->scalarNode('version')
                                ->info('Instrumentation scope version')
                                ->defaultValue('unknown')
                            ->end()
                            ->scalarNode('schema_url')
                                ->info('Schema URL for semantic conventions')
                                ->defaultNull()
                            ->end()
                            ->arrayNode('attributes')
                                ->info('Additional scope attributes')
                                ->normalizeKeys(false)
                                ->useAttributeAsKey('name')
                                ->prototype('variable')->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
            ->end();

        return $treeBuilder;
    }

    private function exporterNode(string $signalType) : ArrayNodeDefinition
    {
        $builder = new TreeBuilder('exporter');
        /** @var ArrayNodeDefinition $node */
        $node = $builder->getRootNode();

        $node
            ->info(\ucfirst($signalType) . ' exporter configuration')
            ->addDefaultsIfNotSet()
            ->children()
                ->enumNode('type')
                    ->values(['memory', 'console', 'void', 'otlp', 'service'])
                    ->defaultValue('void')
                ->end()
                ->scalarNode('service_id')
                    ->info('Custom exporter service ID (only for type: service)')
                    ->defaultNull()
                ->end()
                ->arrayNode('otlp')
                    ->info('OTLP exporter configuration (only for type: otlp)')
                    ->children()
                        ->arrayNode('transport')
                            ->info('OTLP transport configuration')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->enumNode('type')
                                    ->values(['curl', 'http', 'grpc', 'service'])
                                    ->defaultValue('curl')
                                ->end()
                                ->scalarNode('endpoint')
                                    ->info('OTLP endpoint URL (required)')
                                    ->isRequired()
                                    ->cannotBeEmpty()
                                ->end()
                                ->integerNode('timeout')
                                    ->info('Request timeout in seconds')
                                    ->defaultValue(30)
                                    ->min(1)
                                ->end()
                                ->arrayNode('headers')
                                    ->info('Additional HTTP headers')
                                    ->normalizeKeys(false)
                                    ->useAttributeAsKey('name')
                                    ->prototype('scalar')->end()
                                ->end()
                                ->integerNode('connect_timeout')
                                    ->info('Connection timeout in seconds (only for curl)')
                                    ->defaultValue(10)
                                    ->min(1)
                                ->end()
                                ->booleanNode('compression')
                                    ->info('Enable automatic response decompression (only for curl)')
                                    ->defaultFalse()
                                ->end()
                                ->booleanNode('follow_redirects')
                                    ->info('Follow HTTP redirects (only for curl)')
                                    ->defaultTrue()
                                ->end()
                                ->integerNode('max_redirects')
                                    ->info('Maximum number of redirects to follow (only for curl)')
                                    ->defaultValue(3)
                                    ->min(0)
                                ->end()
                                ->scalarNode('proxy')
                                    ->info('Proxy server URL (only for curl, e.g., "http://proxy:8080")')
                                    ->defaultNull()
                                ->end()
                                ->booleanNode('ssl_verify_peer')
                                    ->info('Verify SSL peer certificate (only for curl)')
                                    ->defaultTrue()
                                ->end()
                                ->booleanNode('ssl_verify_host')
                                    ->info('Verify SSL host name (only for curl)')
                                    ->defaultTrue()
                                ->end()
                                ->scalarNode('ssl_cert_path')
                                    ->info('Path to SSL client certificate (only for curl)')
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('ssl_key_path')
                                    ->info('Path to SSL client private key (only for curl)')
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('ca_info_path')
                                    ->info('Path to CA certificate bundle (only for curl)')
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('http_client_service_id')
                                    ->info('PSR-18 HTTP client service ID (only for http transport)')
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('request_factory_service_id')
                                    ->info('PSR-17 request factory service ID (only for http transport)')
                                    ->defaultNull()
                                ->end()
                                ->scalarNode('stream_factory_service_id')
                                    ->info('PSR-17 stream factory service ID (only for http transport)')
                                    ->defaultNull()
                                ->end()
                                ->booleanNode('insecure')
                                    ->info('Allow insecure connections (only for grpc)')
                                    ->defaultTrue()
                                ->end()
                                ->scalarNode('service_id')
                                    ->info('Custom transport service ID (only for type: service)')
                                    ->defaultNull()
                                ->end()
                                ->arrayNode('serializer')
                                    ->info('Serializer configuration')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->enumNode('type')
                                            ->values(['json', 'protobuf', 'service'])
                                            ->defaultValue('json')
                                        ->end()
                                        ->scalarNode('service_id')
                                            ->info('Custom serializer service ID (only for type: service)')
                                            ->defaultNull()
                                        ->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
            ->end();

        return $node;
    }

    private function innerProcessorNode(string $signalType) : ArrayNodeDefinition
    {
        $builder = new TreeBuilder('inner_processor');
        /** @var ArrayNodeDefinition $node */
        $node = $builder->getRootNode();

        $childProcessorTypes = ['memory', 'batching', 'passthrough', 'void', 'service'];

        $node
            ->info('Inner processor configuration for severity_filtering (only for type: severity_filtering)')
            ->children()
                ->enumNode('type')
                    ->values($childProcessorTypes)
                    ->isRequired()
                ->end()
                ->integerNode('batch_size')
                    ->info('Batch size for batching processor')
                    ->defaultValue(512)
                    ->min(1)
                ->end()
                ->scalarNode('service_id')
                    ->info('Custom processor service ID (only for type: service)')
                    ->defaultNull()
                ->end()
                ->append($this->exporterNode($signalType))
            ->end();

        return $node;
    }

    private function processorNode(string $signalType) : ArrayNodeDefinition
    {
        $builder = new TreeBuilder('processor');
        /** @var ArrayNodeDefinition $node */
        $node = $builder->getRootNode();

        $processorTypes = ['composite', 'memory', 'batching', 'passthrough', 'void', 'service'];
        $childProcessorTypes = ['memory', 'batching', 'passthrough', 'void', 'service'];

        if ($signalType === 'log') {
            $processorTypes[] = 'severity_filtering';
            $childProcessorTypes[] = 'severity_filtering';
        }

        $node
            ->info(\ucfirst($signalType) . ' processor configuration')
            ->addDefaultsIfNotSet()
            ->children()
                ->enumNode('type')
                    ->values($processorTypes)
                    ->defaultValue('void')
                ->end()
                ->integerNode('batch_size')
                    ->info('Batch size for batching processor')
                    ->defaultValue(512)
                    ->min(1)
                ->end()
                ->scalarNode('service_id')
                    ->info('Custom processor service ID (only for type: service)')
                    ->defaultNull()
                ->end()
                ->enumNode('minimum_severity')
                    ->info('Minimum severity level for severity_filtering processor (only for log processors)')
                    ->values(['trace', 'debug', 'info', 'warn', 'error', 'fatal'])
                    ->defaultValue('info')
                ->end()
                ->arrayNode('processors')
                    ->info('Array of processor configurations (only for type: composite)')
                    ->arrayPrototype()
                        ->children()
                            ->enumNode('type')
                                ->values($childProcessorTypes)
                                ->isRequired()
                            ->end()
                            ->integerNode('batch_size')
                                ->defaultValue(512)
                                ->min(1)
                            ->end()
                            ->scalarNode('service_id')
                                ->defaultNull()
                            ->end()
                            ->enumNode('minimum_severity')
                                ->values(['trace', 'debug', 'info', 'warn', 'error', 'fatal'])
                                ->defaultValue('info')
                            ->end()
                            ->append($this->exporterNode($signalType))
                            ->append($this->innerProcessorNode($signalType))
                        ->end()
                    ->end()
                ->end()
                ->append($this->exporterNode($signalType))
                ->append($this->innerProcessorNode($signalType))
            ->end();

        return $node;
    }
}
