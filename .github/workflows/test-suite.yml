name: Test Suite

on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'src/adapter/**'
      - 'src/bridge/**'
      - 'src/core/**'
      - 'src/cli/**'
      - 'src/lib/**'
      - 'tools/**'
      - 'examples/**'
      - 'composer.lock'
  push:
    branches: [ 1.x ]
    paths-ignore:
      - 'CHANGELOG.md'

# See https://stackoverflow.com/a/72408109
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  windows-tests:
    uses: ./.github/workflows/job-windows-tests.yml

  website-tests:
    uses: ./.github/workflows/job-website-tests.yml

  libraries-tests:
    uses: ./.github/workflows/job-libraries-tests.yml
    secrets: inherit

  mutation-tests:
    uses: ./.github/workflows/job-mutation-tests.yml

  etl-core-tests:
    needs: [libraries-tests]
    uses: ./.github/workflows/job-etl-core-tests.yml
    secrets: inherit

  extension-tests:
    needs: [libraries-tests]
    uses: ./.github/workflows/job-extension-tests.yml

  benchmark-tests:
    needs: [libraries-tests]
    uses: ./.github/workflows/job-benchmark-tests.yml

  components-tests:
    needs: [etl-core-tests]
    uses: ./.github/workflows/job-components-tests.yml
    secrets: inherit